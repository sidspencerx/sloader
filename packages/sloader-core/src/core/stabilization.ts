import { CoreCapabilities } from '../types';

export async function waitForStabilization(_caps: CoreCapabilities): Promise<void> {
  // Minimal placeholder: could poll DOM size or mutation count.
  return;
}
